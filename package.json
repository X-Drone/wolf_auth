{
  "name": "wolf_auth",
  "version": "1.1.0",
  "main": "index.js",
  "scripts": {
    "install-client": "cd client && npm install",
    "install-server": "cd server/backend && python -m venv venv && venv\\Scripts\\pip install -r requirements.txt",
    "install-all": "npm run install-client && npm run install-server",

    "client": "cd client && npm start",
    "server": "cd server/backend/src && ..\\venv\\Scripts\\python -m uvicorn main:app --reload --host 0.0.0.0 --port 3003",

    "dev": "concurrently \"npm run client\" \"npm run server\"",

    "build": "concurrently \"cd client && npm run build\" \"cd server/backend && docker-compose build\"",
    "start": "cd server/backend && docker-compose down && docker-compose up"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/X-Drone/wolf_auth.git"
  },
  "author": "X-Drone",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/X-Drone/wolf_auth/issues"
  },
  "homepage": "https://github.com/X-Drone/wolf_auth#readme",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
